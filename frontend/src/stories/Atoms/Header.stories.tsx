import React from "react";
import { Title } from "../../components/Header/Header";
import { storiesOf } from "@storybook/react";

storiesOf("Atoms", module).add("HeaderTitle", () => <Title>header title</Title>);
